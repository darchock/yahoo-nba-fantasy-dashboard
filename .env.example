# Yahoo Fantasy Dashboard - Environment Variables
# ================================================
# Copy this file to .env and fill in your values

# -----------------
# Yahoo OAuth Credentials
# -----------------
# These come from the Yahoo Developer Console (https://developer.yahoo.com/apps/)
# They allow our app to authenticate users via Yahoo and access their fantasy data.

# YAHOO_CLIENT_ID: Public identifier for our app (safe to share, like a username)
YAHOO_CLIENT_ID=your_client_id_here

# YAHOO_CLIENT_SECRET: Private key for our app (NEVER share this, like a password)
YAHOO_CLIENT_SECRET=your_client_secret_here

# YAHOO_REDIRECT_URI: Where Yahoo sends users after they authorize our app
# This URL must EXACTLY match what's registered in the Yahoo Developer Console.
# Flow: User clicks login -> Yahoo auth page -> User approves -> Yahoo redirects here
# The FastAPI backend receives the authorization code at this URL.
YAHOO_REDIRECT_URI=https://localhost:8080/callback

# -----------------
# Application Settings
# -----------------

# APP_SECRET_KEY: Used to sign/encrypt session cookies and JWT tokens
# IMPORTANT: Change this to a random string in production!
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET_KEY=generate-a-random-secret-key-here

# DEBUG: Enable debug mode (more verbose logging, relaxed security for local dev)
# Set to "false" in production
DEBUG=true

# FRONTEND_URL: Where to redirect users after successful OAuth login
# This is the Streamlit dashboard URL. After Yahoo auth completes, users land here.
# Flow: Yahoo -> FastAPI callback -> redirect to FRONTEND_URL with auth code
FRONTEND_URL=http://localhost:8501

# API_BASE_URL: The FastAPI backend URL that the Streamlit frontend calls
# Streamlit makes API requests to this URL for data and authentication
API_BASE_URL=https://localhost:8080

# VERIFY_SSL: Whether to verify SSL certificates when Streamlit calls the API
# Set to "false" for local development with self-signed certificates
# Set to "true" in production with proper SSL certificates
VERIFY_SSL=false

# -----------------
# Database
# -----------------

# DATABASE_URL: Connection string for the database
# SQLite for local development (file-based, no server needed)
# For production, use PostgreSQL: postgresql://user:password@host:5432/dbname
DATABASE_URL=sqlite:///./data/fantasy.db

# -----------------
# Scheduler Settings (for future background job feature)
# -----------------

# What time to run daily data refresh jobs (24-hour format, UTC)
# Default: 4:00 AM UTC
SCHEDULER_DAILY_HOUR=4
SCHEDULER_DAILY_MINUTE=0
